# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

```bash
pnpm install          # Install dependencies
pnpm run build        # Compile TypeScript (tsc) → dist/
pnpm run watch        # Compile with --watch
pnpm test             # Run all tests (Jest)
pnpm test -- __tests__/auto-cmd/config.test.ts   # Run a single test file
pnpm test -- --testNamePattern="pattern"          # Run tests matching name
```

## Architecture

Monorepo of independent CLI tools, each under `src/<tool>/`, compiled to `dist/<tool>/` and exposed via `bin` in package.json. All tools are CommonJS TypeScript (`"type": "commonjs"`, target ES2022).

### Tools

The table below is generated from `package.json`'s `bin` field. Do not edit between
the markers; run `node scripts/gen-tools-table.cjs --write` after adding/removing a tool.
`pnpm test` runs `--check` (via `__tests__/scripts/tools-table.test.ts`) and fails on drift.

<!-- TOOLS-TABLE:BEGIN (generated by scripts/gen-tools-table.cjs --write; do not edit between markers) -->

| Tool | Entry | Purpose |
|------|-------|---------|
| **auto-cmd** | `src/auto-cmd/index.ts` | Scheduled command execution (JSON/YAML config, time-based triggers, once/repeat modes) |
| **sync-editor** | `src/sync-editor/index.ts` | Bidirectional settings sync across VSCode/Cursor/Trae (settings, keybindings, extensions) |
| **exec-recursive** | `src/exec-recursive/index.ts` | DFS recursive command execution with depth control |
| **claude-usage** | `src/claude-usage/index.ts` | Claude API usage reporting (Anthropic OAuth, watch/json modes) |
| **claude-task-runner** | `src/claude-task-runner/index.ts` | Automated Claude Code task execution with usage-adaptive parallelism and Feishu notification |
| **claude-task-loop** | `src/claude-task-loop/index.ts` | Loop Claude tasks from JSON config with variable interpolation |
| **codex-usage** | `src/codex-usage/index.ts` | Codex / ChatGPT usage reporting from ~/.codex/auth.json |
| **minimax-usage** | `src/minimax-usage/index.ts` | MiniMax Token Plan usage reporting, optional Feishu notify/poll |
| **llm-gated-run** | `src/llm-gated-run/index.ts` | Run registered tasks gated by a provider window linear budget (minimax) |
| **codex-task-runner** | `src/codex-task-runner/index.ts` | Automated Codex CLI task batching, concurrency driven by codex-usage |
| **codex-task-loop** | `src/codex-task-loop/index.ts` | Loop Codex tasks from JSON config with variable interpolation |
| **git-pull-poll** | `src/git-pull-poll/index.ts` | Poll git pull --ff-only to keep a repo up to date (drives post-merge rebuild) |
| **boot-tasks** | `src/boot-tasks/index.ts` | Launch configured background tasks on boot/startup |
| **skill-doctor** | `src/skill-doctor/index.ts` | Static lint/health check for a Claude skills repo (cross-platform, pure Node) |
| **skillshare-sync-notify** | `src/skillshare-sync-notify/index.ts` | Run skillshare sync and report the result to Feishu |
| **system-status** | `src/system-status/index.ts` | Collect and report local system status |
| **knowledge-sync** | `src/knowledge-sync/index.ts` | Sync ~/Documents/knowledge into LLM Wiki sources (once / watch modes) |
| **zai-usage** | `src/zai-usage/index.ts` | Z.ai (Zhipu) Coding Plan usage reporting via HTTP, with Feishu notification |
| **zai-watch** | `src/zai-watch/index.ts` | Poll a target until OK (N consecutive probes) then report to Feishu and exit; supports authenticated HTTP probes and --once/--dry-run |
| **usage-report** | `src/usage-report/index.ts` | Aggregate multi-provider LLM usage into a single report |

<!-- TOOLS-TABLE:END -->

### Key patterns

- CLI tools use **commander** for argument parsing
- Config files support both JSON and YAML (via `yaml` package)
- Tests live in `__tests__/<tool>/` mirroring `src/<tool>/` structure
- Jest uses `ts-jest` preset with `@/` path alias mapped to `src/`
- Language: Chinese (README, comments, commit messages are in Chinese)
